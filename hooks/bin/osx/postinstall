#!/bin/sh

# duti doesn't automatically read this
if [ -f ~/.duti ]; then
  duti ~/.duti
fi

# TODO: broken
# use homebrew-installed shell
LINE='/usr/local/bin/zsh'
FILE=/etc/shells
sudo grep -qF "$LINE" "$FILE" || sudo bash -c 'echo "$LINE" > $FILE'
sudo chsh -s $LINE $USER

$DOT_PATH/hooks/bin/osx/configure
